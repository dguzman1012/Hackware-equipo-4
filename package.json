{
  "name": "novia-de-gaucho",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter server --filter web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "sim:esp32": "pnpm --filter server sim:esp32",
    "dev:twitter": "pnpm --filter twitter-bot dev"
  }
}
